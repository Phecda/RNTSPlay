import { Theme } from 'react-native-elements';

const RNEThemes: {
  [name: string]: Theme;
} = {
  light: {
    colors: {
      primary: '#000000',
    },
  },
  dark: {
    colors: {
      primary: '#FFFFFF',
    },
  },
};

export default RNEThemes;
